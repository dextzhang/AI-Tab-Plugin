# AI2tab 更新日志

## v3.2.0 (2026-05-27)

- **解除站点逻辑耦合**：从 `content.js` 中彻底剥离特定站点（豆包）的定制代码，改由 `ai-sites.js` 中的声明式 Hook 驱动，content.js 变回纯粹的通用调度器。
- **并发控制优化**：在 `background.js` 中将完全并行发送优化为限制并发数为 2 的轻量异步任务队列，避免同开多标签时浏览器卡顿与选择器失效。
- **实时发送状态反馈**：Popup 与 Background 之间建立实时进度通道。在发送文本或图片请求时，各站点旁会实时显示其当前状态（如“等待中...”、“重载中...”、“已成功发送”等），彻底告别界面假死感。
- **版本管理规范化**：`content.js` 统一动态读取 manifest 的扩展版本，项目各文件版本号统一同步至 `v3.2.0`。

## v3.0.2 (2026-05-19)

- 发送前按站点去重，同一 AI 站点打开多个标签时只选择一个最合适的标签发送，避免同一问题重复发送到多个同站点页面。
- 发送前安静检查目标标签页状态。休眠或未加载完成的标签会优先在后台 reload 并等待完成，不再主动切换到目标标签。
- 对输入框查找失败或消息通道断开增加一次后台 reload 重试，尽量保持非 UI 打扰的发送方式。
- 如果后台重试仍失败，会短暂激活目标标签做一次前台兜底发送，随后恢复原来的活动标签；兜底失败后才记录失败。

## v3.0.1 (2026-05-15)

- 将 Popup 主视觉调整为暖灰 + 浅金色拟物风格。
- 去除主要按钮、选框和选中态的蓝色主色与渐变，统一改为浅金/暖白凸起控件。
- 重新生成 `AI2` 扩展图标，改为浅金色拟态图标。

## v3.0.0 (2026-05-15)

- 重构发送核心：新增共享站点配置 `ai-sites.js`，让 popup、background 和 content script 使用同一份域名、权限和适配器定义。
- 重写页面端发送流程，统一为“进入新对话 -> 查找输入框 -> 写入提示词 -> 提交 -> 验证发送效果”。
- 新对话策略升级为 URL、按钮、快捷键三层 fallback，并对 Kimi 这类旧会话风险较高的站点保留强制保护。
- 扩展权限覆盖补齐 ChatGPT、Gemini、Qwen、豆包、Kimi 等子域名，权限失败日志会提示当前域名。
- Popup 界面改为轻量毛玻璃风格，修复中文乱码文案，改善按钮、日志和状态区的质感。
- 新增 `AI2` 文字图标；放宽 Qwen 发送后的误报判断，并强制 Grok、豆包文本发送前进入 fresh chat URL。
- 在文本发送下方新增站点开关和模式选择：ChatGPT、Gemini、Grok、千问/Qwen、豆包、Kimi 均可单独启用/关闭并选择偏好模式。模式切换失败时会继续按当前页面模式发送。
- 按真实截图将模式切换改为站点策略：豆包、Gemini、Kimi、ChatGPT、Grok 先打开当前模式菜单再选择目标项；千问/Qwen 使用“思考”开关点亮/熄灭。

## v2.0.0 (2026-05-05)

- 将文本同步流程标记为稳定里程碑。
- ChatGPT、Gemini、Grok、通义千问 / Qwen、豆包、Kimi 的文本同步发送流程已基本可用。
- Kimi 发送前会优先进入新对话，避免把新提示词接到旧会话后面。
- 通义千问 / Qwen 和 Kimi 的输入流程改为单次写入，减少重复粘贴、重复插入和提交前二次写入问题。
- 后台 service worker 负责跨标签页发送和持久化日志，弹窗关闭后仍能记录结果。
- 后续重点转向图片生成模式、更多站点适配和 UI 体验。

## v1.6.2 (2026-05-05)

- 修复 Kimi 旧会话页发送前没有真正进入新对话的问题。
- 收紧 Kimi 新建对话选择器，移除容易点到历史会话的宽泛 `/chat` 链接匹配。
- 如果 Kimi 仍停留在旧会话 URL，会停止发送，避免追加到旧对话。
- 修复通义千问 / Qwen 和 Kimi 文本重复写入问题。

## v1.6.1 (2026-05-05)

- 增加“提交前清空并只写入一次”的站点专用流程。
- 该流程只应用于通义千问 / Qwen 和 Kimi，避免影响已稳定的 ChatGPT、Gemini、Grok。
- 新增 `clearInput` 工具函数，支持 `textarea`、`input` 和 `contenteditable`。

## v1.6.0 (2026-05-04)

- 形成阶段性稳定版本：弹窗中文恢复、后台发送、持久化日志和多站点适配完成整合。
- 文本模式支持 ChatGPT、Gemini、Grok、通义千问 / Qwen、豆包、Kimi。
- 图片模式支持 ChatGPT、Gemini、Grok Imagine、通义千问 / Qwen、豆包、Kimi。
- 后台 service worker 执行发送任务，降低 popup 失焦关闭导致任务中断的概率。
- 增强发送日志，记录目标数、成功数和每个站点失败原因。
- 增加 Shadow DOM 查询、Kimi / Qwen / 豆包 / Grok 的关键选择器。

## v1.5.x (2026-05-04)

- 重建 popup UI，修复中文标签和按钮 ID 损坏。
- 修复 content script 重复注入和旧处理器无法热更新的问题。
- 注入顺序固定为先 `utils.js` 后 `content.js`。
- 扩展 Grok、Qwen / 通义千问、豆包、Kimi 的 URL 匹配和权限。
- 增加持久化发送日志。
- 将真实发送流程从 `popup.js` 移到 `background.js`。
- 多次修复 Qwen、Kimi、豆包输入重复、按钮触发和错误可见性问题。

## v1.4.x and earlier

- 初始多 AI 同步发送原型。
- 新增 `utils.js`。
- 增加文本和图片请求流程。
